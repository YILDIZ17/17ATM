namespace ATM.Bll
{
    public class StartBll
    {

        public static void Start(Model.UserInfo user) // Method to start the program
        {
            bool retry = true;
            while (retry) // While the user doesn't choose 4, we display the user interface
            {
            }
        }
    }
}
